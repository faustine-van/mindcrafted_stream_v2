-- =====================================================
-- MIGRATION: Rename status → tmdb_status
-- Run in Supabase SQL Editor
-- =====================================================

-- 1. Rename the column
ALTER TABLE watchlist_items
  RENAME COLUMN status TO tmdb_status;

-- 2. Update the index name to match (optional but keeps things tidy)
DROP INDEX IF EXISTS idx_watchlist_status;
CREATE INDEX IF NOT EXISTS idx_watchlist_tmdb_status
  ON watchlist_items(user_id, tmdb_status);